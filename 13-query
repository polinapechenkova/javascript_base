function queryString(object) {
  let result = [];

  for (let key in object) {
    result.push(`${key}=${object[key]}`);
  }

  return result.join("&");
}

const queryParametrs = {
  search: "Вася",
  take: 10,
};

console.log(queryString(queryParametrs));